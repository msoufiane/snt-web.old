version: "2"
plugins:
  eslint:
    enabled: true
    channel: "eslint-4"
    config:
        config: .eslintrc
        extensions:
        - .es6
        - .js
check:
  method-lines:
    enabled: false

exclude_patterns:
  - "*.config.*"
  - "tools/*"
  - "package.json"
